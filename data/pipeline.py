# Pipeline For Data

def retrieve_pipeline_data(pipeline):
    """
    Retrieve data from a given pipeline.
    Returns:
    The data retrieved from the pipeline.
    """
    data = []
    for item in pipeline:
        data.append(item)
    return data